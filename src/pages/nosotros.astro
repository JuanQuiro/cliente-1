---
import MainLayout from '../layouts/MainLayout.astro';
import { Image } from 'astro:assets';
import aboutImage from '../assets/nosotros/nosotros.jpg';
import heroUnoImg from '../assets/image-hero-index/hero-uno.jpg';
import heroDosImg from '../assets/image-hero-index/hero-dos.jpg';
import heroTresImg from '../assets/image-hero-index/hero-tres.jpg';

const pageTitle = 'Sobre Nosotros';
const pageDescription = 'Conoce nuestra historia, misión, visión y valores. Descubre por qué somos tu mejor opción en servicios de mantenimiento.';

// Team members data
const teamMembers = [
  {
    name: 'Juan Pérez',
    position: 'Fundador & CEO',
    image: heroUnoImg,
    bio: 'Con más de 15 años de experiencia en el sector de mantenimiento, Juan fundó la empresa con la visión de ofrecer servicios de calidad excepcional.',
    social: {
      twitter: '#',
      linkedin: '#',
      email: 'mailto:juan@ejemplo.com'
    }
  },
  {
    name: 'María González',
    position: 'Directora de Operaciones',
    image: heroDosImg,
    bio: 'Especialista en gestión de proyectos con una amplia experiencia en coordinación de equipos y optimización de procesos.',
    social: {
      twitter: '#',
      linkedin: '#',
      email: 'mailto:maria@ejemplo.com'
    }
  },
  {
    name: 'Carlos Rodríguez',
    position: 'Supervisor de Calidad',
    image: heroTresImg,
    bio: 'Encargado de garantizar que cada servicio cumpla con los más altos estándares de calidad y satisfacción del cliente.',
    social: {
      twitter: '#',
      linkedin: '#',
      email: 'mailto:carlos@ejemplo.com'
    }
  },
  {
    name: 'Ana Martínez',
    position: 'Jefa de Servicio al Cliente',
    image: heroUnoImg,
    bio: 'Apasionada por brindar la mejor experiencia al cliente y asegurar que todas sus necesidades sean atendidas.',
    social: {
      twitter: '#',
      linkedin: '#',
      email: 'mailto:ana@ejemplo.com'
    }
  }
];

// Company stats
const stats = [
  { number: '10+', label: 'Años de Experiencia' },
  { number: '500+', label: 'Clientes Satisfechos' },
  { number: '50+', label: 'Proyectos Completados' },
  { number: '24/7', label: 'Soporte al Cliente' }
];
---

<MainLayout title={pageTitle} description={pageDescription}>
  <!-- Page Header -->
  <section class="page-header" style={`background-image: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), url(${heroDosImg.src});`}>
    <div class="container">
      <h1>Sobre Nosotros</h1>
      <nav class="breadcrumb">
        <a href="/">Inicio</a>
        <span>/</span>
        <span>Nosotros</span>
      </nav>
    </div>
  </section>

  <!-- About Intro -->
  <section class="about-intro">
    <div class="container">
      <div class="about-intro-content">
        <div class="about-intro-text">
          <h2>Jonathan Roofing & Landscaping</h2>
          <p>Nos especializamos en soluciones integrales para hogares y negocios en Puerto Rico, combinando expertise en techos, paisajismo y mantenimiento general.</p>
          <ul class="space-y-1 list-none mt-4">
            <li>✔ Sellado de techos con materiales premium (Eterna, Marin Membrana)</li>
            <li>✔ Mantenimiento de áreas verdes usando equipos profesionales (STIHL, ILLEGNO)</li>
            <li>✔ Servicios de <em>handyman</em> y remodelaciones menores</li>
          </ul>
          <p class="mt-6">Nuestra experiencia nos ha permitido convertirnos en la elección confiable de cientos de clientes que buscan:</p>
          <ul class="list-disc list-inside space-y-1">
            <li><strong>Durabilidad</strong>: Trabajos garantizados que resisten el clima tropical</li>
            <li><strong>Eficiencia</strong>: Respuesta rápida ante emergencias (goteras, cortes urgentes)</li>
            <li><strong>Conexión local</strong>: Entendemos las necesidades únicas de Puerto Rico</li>
          </ul>
          <blockquote class="mt-6 italic text-green-700 border-l-4 border-green-600 pl-4">"No solo entregamos servicios... creamos entornos seguros, funcionales y estéticos para tu familia o negocio"</blockquote>
        </div>
        <div class="about-intro-image">
          <Image src={aboutImage} alt="Nuestro equipo trabajando" width={1920} height={1080} class="w-full h-auto" />
        </div>
      </div>
    </div>
  </section>

  <!-- Mission & Vision -->
  <section class="mission-vision">
    <div class="container">
      <div class="mission-vision-grid">
        <div class="mission-box">
          <div class="icon-box">
            <i class="fas fa-bullseye"></i>
          </div>
          <h3>MISIÓN</h3>
          <p>En Jonathan Roofing & Landscaping, nos comprometemos a brindar soluciones integrales de mantenimiento y construcción con estándares profesionales, utilizando equipos industriales (STIHL, BERNARD) y materiales premium (Eterna, Marin Membrana).</p>
        </div>
        
        <div class="vision-box">
          <div class="icon-box">
            <i class="fas fa-eye"></i>
          </div>
          <h3>VISIÓN</h3>
          <p>Ser referentes en servicios integrales para el hogar y comercio en toda Puerto Rico, reconocidos por:</p>
          <ul class="list-disc list-inside space-y-1 mt-2">
            <li>🏆 Excelencia técnica en sellado de techos, paisajismo y remodelaciones</li>
            <li>🌱 Innovación constante adoptando las mejores prácticas de la industria</li>
            <li>🤝 Relaciones duraderas con clientes que confían en nuestro trabajo</li>
          </ul>
          <p class="mt-4 italic text-green-700">"Queremos ser el primer nombre que viene a mente cuando piensan en ‘mantenimiento profesional’ en la isla"</p>
        </div>
      </div>
    </div>
  </section>

  <!-- Values -->
  <section class="values-section">
    <div class="container">
      <div class="section-header">
        <h2>VALORES</h2>
        <p class="text-gray-700">Nuestros principios fundamentales</p>
      </div>
      
      <div class="values-grid">
        <div class="value-card">
          <div class="value-icon"><i class="fas fa-check-circle"></i></div>
          <h3>Honestidad radical</h3>
        </div>

        <div class="value-card">
          <div class="value-icon"><i class="fas fa-check-circle"></i></div>
          <h3>Orgullo boricua</h3>
        </div>

        <div class="value-card">
          <div class="value-icon"><i class="fas fa-check-circle"></i></div>
          <h3>Sostenibilidad real</h3>
        </div>

        <div class="value-card">
          <div class="value-icon"><i class="fas fa-check-circle"></i></div>
          <h3>Pasión por el detalle</h3>
        </div>

        <div class="value-card">
          <div class="value-icon"><i class="fas fa-check-circle"></i></div>
          <h3>Compromiso social</h3>
        </div>
      </div>
    </div>
  </section>

  <!-- Stats Section -->
  <section class="stats-section">
    <div class="container">
      <div class="stats-grid">
        {stats.map(stat => (
          <div class="stat-item">
            <span class="stat-number">{stat.number}</span>
            <span class="stat-label">{stat.label}</span>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- CTA Section -->
  <section class="about-cta" style={`background-image: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.7)), url(${heroTresImg.src});`}>
    <div class="container">
      <div class="cta-content">
        <h2>¿Listo para experimentar la diferencia de trabajar con nosotros?</h2>
        <p>Contáctanos hoy mismo y descubre por qué cientos de clientes confían en nuestros servicios.</p>
        <div class="cta-buttons">
          <a href="/contacto" class="btn btn-light">Contáctanos</a>
          <a href="/servicios" class="btn btn-outline-light">Ver Servicios</a>
        </div>
      </div>
    </div>
  </section>
</MainLayout>

<style>
  /* Page Header */
  .page-header {
    
    background-size: cover;
    background-position: center;
    color: white;
    text-align: center;
    padding: 8rem 0 4rem;
    position: relative;
  }
  
  .page-header h1 {
    font-size: 3rem;
    margin-bottom: 1rem;
    color: white;
  }
  
  .breadcrumb {
    display: flex;
    justify-content: center;
    align-items: center;
    gap: 0.5rem;
    font-size: 1.1rem;
  }
  
  .breadcrumb a {
    color: var(--accent);
    text-decoration: none;
    transition: color 0.3s ease;
  }
  
  .breadcrumb a:hover {
    color: white;
  }
  
  .breadcrumb span {
    opacity: 0.7;
  }
  
  /* About Intro */
  .about-intro {
    padding: 6rem 0;
    background-color: white;
  }
  
  .about-intro-content {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 4rem;
    align-items: center;
  }
  
  .about-intro-text h2 {
    font-size: 2.5rem;
    color: var(--primary);
    margin-bottom: 1.5rem;
    position: relative;
    display: inline-block;
  }
  
  .about-intro-text h2::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 0;
    width: 80px;
    height: 3px;
    background-color: var(--accent);
  }
  
  .about-intro-text p {
    color: var(--gray);
    line-height: 1.8;
    margin-bottom: 1.5rem;
    font-size: 1.1rem;
  }
  
  .about-intro-image {
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease;
  }
  
  .about-intro-image:hover {
    transform: translateY(-10px);
  }
  
  .about-intro-image img {
    width: 100%;
    height: auto;
    display: block;
    transition: transform 0.5s ease;
  }
  
  .about-intro-image:hover img {
    transform: scale(1.05);
  }
  
  /* Mission & Vision */
  .mission-vision {
    padding: 6rem 0;
    background-color: #f9fafb;
  }
  
  .mission-vision-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2.5rem;
  }
  
  .mission-box,
  .vision-box {
    background: white;
    padding: 3rem 2.5rem;
    border-radius: 12px;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
    text-align: center;
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  
  .mission-box:hover,
  .vision-box:hover {
    transform: translateY(-10px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
  }
  
  .mission-box {
    border-top: 4px solid var(--accent);
  }
  
  .vision-box {
    border-top: 4px solid var(--primary);
  }
  
  .icon-box {
    width: 80px;
    height: 80px;
    background-color: rgba(231, 76, 60, 0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
    font-size: 2rem;
    color: var(--accent);
    transition: all 0.3s ease;
  }
  
  .vision-box .icon-box {
    color: var(--primary);
    background-color: rgba(34, 47, 62, 0.1);
  }
  
  .mission-box h3,
  .vision-box h3 {
    font-size: 1.8rem;
    margin-bottom: 1.5rem;
    color: var(--primary);
  }
  
  .vision-box h3 {
    color: var(--primary);
  }
  
  .mission-box p,
  .vision-box p {
    color: var(--gray);
    line-height: 1.8;
    margin: 0;
  }
  
  /* Values Section */
  .values-section {
    padding: 6rem 0;
    background-color: white;
  }
  
  .section-header {
    text-align: center;
    max-width: 800px;
    margin: 0 auto 4rem;
  }
  
  .section-header h2 {
    font-size: 2.5rem;
    color: var(--primary);
    margin-bottom: 1rem;
    position: relative;
    display: inline-block;
  }
  
  .section-header h2::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 80px;
    height: 3px;
    background-color: var(--accent);
  }
  
  .section-header p {
    color: var(--gray);
    font-size: 1.1rem;
    line-height: 1.7;
  }
  
  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
    gap: 2rem;
  }
  
  .value-card {
    background: #f9fafb;
    border-radius: 12px;
    padding: 2.5rem 2rem;
    text-align: center;
    transition: all 0.3s ease;
    border: 1px solid #f0f0f0;
  }
  
  .value-card:hover {
    background: white;
    transform: translateY(-10px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.08);
  }
  
  .value-icon {
    width: 80px;
    height: 80px;
    background-color: rgba(231, 76, 60, 0.1);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    margin: 0 auto 1.5rem;
    font-size: 1.8rem;
    color: var(--accent);
    transition: all 0.3s ease;
  }
  
  .value-card:hover .value-icon {
    background-color: var(--accent);
    color: white;
    transform: rotateY(180deg);
  }
  
  .value-card h3 {
    font-size: 1.4rem;
    margin-bottom: 1rem;
    color: var(--primary);
  }
  
  .value-card p {
    color: var(--gray);
    line-height: 1.7;
    margin: 0;
  }
  
  /* Team Section */
  .team-section {
    padding: 6rem 0;
    background-color: #f9fafb;
  }
  
  .team-grid {
    display: grid;
    grid-template-columns: repeat(auto-fill, minmax(250px, 1fr));
    gap: 2.5rem;
  }
  
  .team-member {
    background: white;
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.05);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
  }
  
  .team-member:hover {
    transform: translateY(-10px);
    box-shadow: 0 15px 40px rgba(0, 0, 0, 0.1);
  }
  
  .member-image {
    position: relative;
    height: 300px;
    overflow: hidden;
  }
  
  .member-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.5s ease;
  }
  
  .team-member:hover .member-image img {
    transform: scale(1.05);
  }
  
  .member-social {
    position: absolute;
    bottom: 0;
    left: 0;
    width: 100%;
    padding: 1.5rem;
    background: linear-gradient(transparent, rgba(0, 0, 0, 0.7));
    display: flex;
    justify-content: center;
    gap: 1rem;
    opacity: 0;
    transition: opacity 0.3s ease;
  }
  
  .team-member:hover .member-social {
    opacity: 1;
  }
  
  .member-social a {
    display: flex;
    align-items: center;
    justify-content: center;
    width: 40px;
    height: 40px;
    background: var(--accent);
    color: white;
    border-radius: 50%;
    transition: all 0.3s ease;
  }
  
  .member-social a:hover {
    background: var(--accent-dark);
    transform: translateY(-3px);
  }
  
  .member-info {
    padding: 2rem;
    text-align: center;
  }
  
  .member-info h3 {
    font-size: 1.4rem;
    margin-bottom: 0.5rem;
    color: var(--primary);
  }
  
  .position {
    display: block;
    color: var(--accent);
    font-weight: 600;
    margin-bottom: 1rem;
    font-size: 0.95rem;
  }
  
  .member-info p {
    color: var(--gray);
    font-size: 0.95rem;
    line-height: 1.7;
    margin: 0;
  }
  
  /* Stats Section */
  .stats-section {
    padding: 6rem 0;
    background: linear-gradient(135deg, var(--primary) 0%, #1a2639 100%);
    color: white;
  }
  
  .stats-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    text-align: center;
  }
  
  .stat-item {
    padding: 2rem;
  }
  
  .stat-number {
    display: block;
    font-size: 3.5rem;
    font-weight: 700;
    margin-bottom: 0.5rem;
    color: white;
    line-height: 1;
  }
  
  .stat-label {
    font-size: 1.1rem;
    opacity: 0.9;
  }
  
  /* CTA Section */
  .about-cta {
    padding: 6rem 0;
    
    background-size: cover;
    background-position: center;
    background-attachment: fixed;
    color: white;
    text-align: center;
    position: relative;
  }
  
  .about-cta::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: none; /* Pattern removed - using solid background */
    opacity: 0.1;
    z-index: 1;
  }
  
  .cta-content {
    position: relative;
    z-index: 2;
    max-width: 800px;
    margin: 0 auto;
  }
  
  .about-cta h2 {
    font-size: 2.5rem;
    margin-bottom: 1.5rem;
    color: white;
  }
  
  .about-cta p {
    font-size: 1.2rem;
    margin-bottom: 2.5rem;
    opacity: 0.9;
  }
  
  .cta-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    flex-wrap: wrap;
  }
  
  .btn-light {
    background-color: white;
    color: var(--primary);
    border: 2px solid white;
    padding: 0.8rem 2.5rem;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 50px;
    transition: all 0.3s ease;
  }
  
  .btn-light:hover {
    background-color: transparent;
    color: white;
  }
  
  .btn-outline-light {
    background-color: transparent;
    color: white;
    border: 2px solid rgba(255, 255, 255, 0.3);
    padding: 0.8rem 2.5rem;
    font-size: 1.1rem;
    font-weight: 600;
    border-radius: 50px;
    transition: all 0.3s ease;
  }
  
  .btn-outline-light:hover {
    background-color: white;
    color: var(--primary);
    border-color: white;
  }
  
  /* Responsive Styles */
  @media (max-width: 1200px) {
    .team-grid {
      grid-template-columns: repeat(2, 1fr);
    }
    
    .stats-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (max-width: 992px) {
    .about-intro-content {
      grid-template-columns: 1fr;
      gap: 3rem;
    }
    
    .about-intro-text {
      order: 2;
    }
    
    .about-intro-image {
      order: 1;
      max-width: 600px;
      margin: 0 auto;
    }
    
    .mission-vision-grid {
      grid-template-columns: 1fr;
      max-width: 600px;
      margin: 0 auto;
    }
    
    .values-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }
  
  @media (max-width: 768px) {
    .page-header h1 {
      font-size: 2.5rem;
    }
    
    .section-header h2 {
      font-size: 2.2rem;
    }
    
    .about-cta h2 {
      font-size: 2.2rem;
    }
  }
  
  @media (max-width: 576px) {
    .page-header {
      padding: 6rem 0 3rem;
    }
    
    .page-header h1 {
      font-size: 2rem;
    }
    
    .breadcrumb {
      font-size: 1rem;
    }
    
    .about-intro-text h2 {
      font-size: 2rem;
    }
    
    .section-header h2 {
      font-size: 1.8rem;
    }
    
    .values-grid {
      grid-template-columns: 1fr;
    }
    
    .team-grid {
      grid-template-columns: 1fr;
      max-width: 400px;
      margin: 0 auto;
    }
    
    .stats-grid {
      grid-template-columns: 1fr;
    }
    
    .about-cta h2 {
      font-size: 1.8rem;
    }
    
    .cta-buttons {
      flex-direction: column;
      align-items: center;
    }
    
    .cta-buttons .btn {
      width: 100%;
      max-width: 250px;
    }
  }
  /* Unificar estilos de la sección de valores y texto general */
  .about-intro p,
  .mission-vision p,
  .mission-vision ul li,
  .values-section p,
  .section-header p,
  .value-card p,
  .team-section p {
    color:#374151; /* gray-700 */
  }
  .about-intro h2,
  .mission-vision h3,
  .value-card h3,
  .section-header h2 {
    color:#111827; /* gray-900 */
  }
  /* Unificar estilos de la sección de valores */
  .values-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
    gap: 1.5rem;
  }
  .value-card {
    background: #f9fafb;
    border-radius: 0.75rem;
    padding: 1.25rem;
    text-align: center;
    box-shadow: 0 2px 6px rgba(0,0,0,0.05);
  }
  .value-card h3 { color:#111827; font-weight:600; margin-top:0.75rem; }
  .value-icon i {
    font-size: 2rem;
    color: #059669;
  }
  /* Ocultar tarjetas heredadas que superen las 5 deseadas */
  .values-grid .value-card:nth-child(n+6) { display:none; }
</style>
